server:
  port: 8082

spring:
  application:
    name: document-service
  security:
    oauth2:
      resourceserver:
        jwt:
          secret-key: super-secret-key-change-me-super-secret-key-change-me
  datasource:
    url: jdbc:mysql://localhost:3308/document_db
    username: document
    password: document
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

management:
  endpoints:
    web:
      exposure:
        include: health,info

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui

logging:
  level:
    root: INFO


